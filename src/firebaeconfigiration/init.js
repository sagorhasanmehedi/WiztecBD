import { initializeApp } from "firebase/app";
import firebaseConfig from "./config";

const firebaseinit = () => {
  initializeApp(firebaseConfig);
};

export default firebaseinit;

// project-803036810633
